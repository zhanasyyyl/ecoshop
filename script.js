// Это файл с скриптами JavaScript для сайта EcoShop
// Функция для открытия внешнего сайта в новом окне
function openExternal(url) {
  window.open(url, "_blank");
}

function mailTo(email) {
  window.open("mailto:" + email);
}
